/*
  - goal: let users select options then show price
  - handling price:
    - store price in a variable
    - price should change when an option is clicked
  - handling options:
    - use checkboxes for options
    - use radio buttons when user can only select one option
    - data can be stored in an object
  - handling display
    - show a list of user selected items and total price
*/

let price = 0;
document.getElementById('price').innerHTML = price;
